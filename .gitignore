# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Precomputed travel-time matrix — regenerated by the r5py pipeline.
# manifest.json + the m/ binaries go together; commit both only for a deploy.
public/data/sf/m/
public/data/sf/manifest.json

# Stray screenshots pasted into the repo root
/arc_labels.png

# Python pipeline downloads & venv
pipeline/python/data/
pipeline/python/.venv/
pipeline/python/__pycache__/
pipeline/python/*.egg-info/

# Sample-pipeline cached downloads (Overpass results)
pipeline/data/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
